[project]
name = "arcsen-mural-mcp"
version = "0.1.0"
description = "MCP server for creating diagrams and processes on Mural boards"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
    "python-dotenv>=1.2.2",
]
